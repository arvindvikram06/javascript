## Image Gallery Lightbox

====

## Description

* This is an image gallery where clicking a thumbnail opens a larger version in a modal/lightbox overlay.

## Features

* added event listeners to detect when a thumbnail is clicked.
* Dynamically update the modal content with the selected image
* Dynamically add or remove .show class to show/hide the modal and add transitions for
smooth effects.
